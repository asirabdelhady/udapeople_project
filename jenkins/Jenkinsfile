pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Build Frontend') {
            steps {
                sh '''
                    cd frontend/
                    npm install
                    npm run build
                '''
            }
        }
    }
}
